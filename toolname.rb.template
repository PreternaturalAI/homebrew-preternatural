class <classname> < Formula
  desc "<classname> CLI Tool"
  homepage "https://github.com/PreternaturalAI/homebrew-preternatural"
  url
  sha256
  version

  def install
    system "unzip", "-o", cached_download
    bin.install "<toolname>/bin/<toolname>"
  end

  test do
    system "#{bin}/<toolname>"
  end
end
